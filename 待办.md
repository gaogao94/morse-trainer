# 待办 / 已知问题（2026-08-04 记录）

> ⚠️ 在这个文件夹做任何事之前，先读完本文件，并主动提醒用户以下待改项。
> 用户明确说过：**先不改**，等用户发话再动。

## 待改问题（用户实机发现，均未修复）

1. **树图右侧空隙太大，连接线看不到**
   - 学习/教学页上方的二叉树，右侧留白过多；
   - 节点之间的连接线（点/划边）显示不出来。
   - 可能方向：缩放后线宽/颜色对比度问题，或 viewBox 边距问题。需要先拿用户实际截图复现。

2. **反馈发不出**
   - 用户实际环境里点"发送反馈"仍失败（页面提示"服务器反馈接口不可用"）。
   - 服务器侧 hermes 微信通道本身已实测打通（服务器 agent 验证过），所以问题多半在：页面访问入口不是 serve.py 伺服的 / 反代或 systemd 未生效 / 前端 fetch 路径与实际部署不匹配。下次先确认用户访问的 URL 和部署拓扑。

3. **反馈页面比例问题**
   - 反馈 tab 的缩放/比例显示异常（与 updateScale 的缩放逻辑可能有关）。

4. **"等好多问题"**
   - 用户提到还有其他问题没一一列举。下次处理前，先请用户补充或自己全面过一遍各 tab 的手机端表现。

## 当前代码状态备忘

- GitHub 最新提交：`eb0a234`（serve.py 默认只监听 127.0.0.1）。
- `serve.py` 默认只监听 `127.0.0.1`（`BIND_HOST` 可覆盖为 `0.0.0.0`）、`feedback.log` 自动 `chmod 600`，`README.md` 已同步 —— 已于 2026-08-08 提交 `eb0a234` 并 push。
- 服务器部署在 `/opt/morse-trainer`，通过 cloudflared 隧道 → `localhost:12100` 访问；微信通道命令：`hermes send --to weixin -q "文本"`。
- 未跟踪文件：`文件夹介绍.md`（用户私有，别动）。本文件 `待办.md` 已纳入 git。
